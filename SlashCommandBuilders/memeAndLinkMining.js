const { prisma } = require(`../prisma/prisma`);

const getMemeMiningCount = (discordId) => {};
const addMemeMiningCount = (discordId) => {};
const getLinkMiningCount = (discordId) => {};
const addLinkMiningCount = (discordId) => {};

module.exports = {
  getMemeMiningCount,
  addMemeMiningCount,
  getLinkMiningCount,
  addLinkMiningCount,
};
